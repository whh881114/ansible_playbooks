---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: consul-server-gd
  namespace: consul
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
    - host: consul-gd.k8s.freedom.org
      http:
        paths:
        - path: /
          backend:
            serviceName: consul-server-gd
            servicePort: 8500